# --- secrets & machine-local state -------------------------------------------
# Unanchored, so these are ignored inside every card directory (3090/, 5090/...).
.env
.ninfer-instance

# --- scratch work, not part of this project ----------------------------------
# Local experiments that happen to share this checkout. Never published.
sandbox/

# --- python env / caches -----------------------------------------------------
.venv/
__pycache__/
*.py[cod]

# --- macOS -------------------------------------------------------------------
.DS_Store
